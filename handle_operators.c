/*
** EPITECH PROJECT, 2022
** codeTree
** File description:
** handle_operators
*/

#include "my.h"

int get_operator_with_priority(char *str, int prio)
{
    // Returns the right operator with the lowest priority
}

char get_operators(char *str)
{
    // Combine this function with get_operator_with_priority to get the operator with the lowest priority first
}

int get_operator_index(parsing_t *parsing, char *buffer, int prio)
{
    // Returns the right operator with the lowest priority (+ - / * %) (becareful with the priority)
    // and returns his index
}
